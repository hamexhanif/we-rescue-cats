# # catapi.key=test-api-key
# # catapi.base-url=https://api.thecatapi.com/v1
# # catapi.fetch-on-startup=false

# # logging.level.io.werescuecats.backend=DEBUG

# # spring.jpa.show-sql=true
# # spring.jpa.properties.hibernate.format_sql=true
# # logging.level.org.hibernate.SQL=DEBUG
# # logging.level.org.hibernate.type.descriptor.sql=TRACE

# # logging.level.root=DEBUG
# # logging.level.io.werescuecats.backend.service=DEBUG

# # # H2 Database configuration
# # spring.datasource.url=jdbc:h2:mem:testdb
# # spring.datasource.driverClassName=org.h2.Driver
# # spring.datasource.username=sa
# # spring.datasource.password=password
# # spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# # spring.h2.console.enabled=true
# # spring.jpa.hibernate.ddl-auto=create-drop

# # # Disable security for testing (if needed)
# # logging.level.org.springframework.security=DEBUG
# # H2 Database configuration
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=false

# # JWT Configuration (dummy values for tests)
# jwt.secret=testsecretthatisatleast32characterslong123456789
# jwt.expiration=86400000

# # Logging
# logging.level.org.springframework.security=DEBUG
# logging.level.org.springframework.web=DEBUG
# logging.level.io.werescuecats.backend=DEBUG

# H2 Database configuration
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# JWT Configuration (dummy values to avoid initialization errors)
jwt.secret=testsecretthatisatleast32characterslong123456789
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN
logging.level.io.werescuecats.backend=INFO